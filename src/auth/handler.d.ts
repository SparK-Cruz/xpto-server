import * as restify from 'restify';
export declare class AuthHandler {
    create(req: any, res: restify.Response): void;
    noop(req: any, res: restify.Response): void;
    destroy(req: any, res: restify.Response): void;
}
